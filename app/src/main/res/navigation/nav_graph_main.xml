<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_main"
    app:startDestination="@id/menuFragment">

    <fragment
        android:id="@+id/menuFragment"
        android:name="com.mmfsin.betweenminds.presentation.menu.MenuFragment"
        android:label="menuFragment"
        tools:layout="@layout/fragment_menu">

        <action
            android:id="@+id/action_to_choose_fragment"
            app:destination="@id/chooseFragment">
            <argument
                android:name="gameType"
                app:argType="string" />
        </action>

    </fragment>

    <fragment
        android:id="@+id/chooseFragment"
        android:name="com.mmfsin.betweenminds.presentation.choose.ChooseFragment"
        android:label="chooseFragment"
        tools:layout="@layout/fragment_choose">
        <action
            android:id="@+id/action_to_room_code_fragment"
            app:destination="@id/roomCodeFragment">
            <argument
                android:name="roomId"
                app:argType="string" />
            <argument
                android:name="gameType"
                app:argType="string" />
        </action>
    </fragment>

    <fragment
        android:id="@+id/roomCodeFragment"
        android:name="com.mmfsin.betweenminds.presentation.choose.roomcode.RoomCodeFragment"
        android:label="roomCodeFragment"
        tools:layout="@layout/fragment_room_code" />

</navigation>